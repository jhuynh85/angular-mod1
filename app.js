(function(){
  angular.module("LunchCheck", [])

  .controller('LunchCheckController', init);

  init.$inject = ['$scope'];
  function init($scope){
    $scope.checkIfToomuch = function () {

    }
  }


})();
